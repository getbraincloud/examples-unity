"use strict";

function main() {
  var response = {};

  var moneyProxy = bridge.getVirtualCurrencyServiceProxy();
  var result = moneyProxy.awardCurrency("gems", data.increaseAmount);
  if(result.status == 200)
  {
    response.getResult = result;
  }

  return response;
}

main();
